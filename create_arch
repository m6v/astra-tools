#!/bin/env bash
# Установка владельца root:root для каталогов и файлов
find root -exec chown root:root {} \;
# Установка бита исполнения для исполняемых файлов
find root -type f -exec file {} \; | grep executable | cut -d':' -f1 | xargs chmod +x
cd root
tar -czvf ../postinst.tar.gz *
